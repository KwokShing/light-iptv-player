node_modules
dist
dist-ssr
*.local
.downloads
.dart_tool
.flutter-plugins
.flutter-plugins-dependencies
.idea
build
*.iml

src-tauri/target

.DS_Store
Thumbs.db

# M3U playlists
*.m3u
*.m3u8

# Custom DASH-capable libmpv build (too large for git; placed manually and
# copied over media_kit's bundled DLL by windows/CMakeLists.txt at build time)
libmpv-2.dll

# Reference sources kept locally for study, not part of this project
dash.js-development/
libdash-master/
