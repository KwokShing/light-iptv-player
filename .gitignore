node_modules
dist
dist-ssr
*.local
.downloads
.dart_tool
.flutter-plugins
.flutter-plugins-dependencies
.idea
build
*.iml

src-tauri/target

.DS_Store
Thumbs.db

# M3U playlists
*.m3u
*.m3u8

# DASH-capable libmpv build. Fetched automatically from a pinned
# zhongfly/mpv-winbuild release by windows/CMakeLists.txt at configure time
# (cached under build/), then installed over media_kit's bundled DLL. Kept out
# of git regardless of where a copy might end up.
libmpv-2.dll

# Reference sources kept locally for study, not part of this project
dash.js-development/
libdash-master/
